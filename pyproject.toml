[project]
name = "smartrag"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "langchain",
    "langchain-google-genai",
    "langchain-chroma",
    "langchain-text-splitters",
    "chromadb",
    "python-dotenv",
    "pypdf",
    "pylint>=4.0.5",
]
