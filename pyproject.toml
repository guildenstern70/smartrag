[project]
name = "smartrag"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "langchain",
    "langchain-google-genai",
    "langchain-community",
    "chromadb",
    "python-dotenv",
    "pypdf",
]
